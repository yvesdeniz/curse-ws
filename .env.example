# Port the relay listens on (nginx proxies wss://websocket.cvrse.lol → this)
WS_PORT=3010

# Secret the bot sends as `Authorization: Bearer <WS_SECRET>` on upgrade
WS_PRODUCER_SECRET=

# Secret dashboards/consumers send on upgrade
WS_CONSUMER_SECRET=
